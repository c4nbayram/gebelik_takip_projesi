name: gebelik_bebek_takip
description: AI destekli gebelik takip mobil uygulaması (Flutter).
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0

flutter:
  uses-material-design: true

